# Task Plan

## Goal

基于 `013.01 video` 的经验，初始化一个更轻量的群体情感视频标注项目，并提前把流程简化、质量控制、日志记录、多模态大模型调用规范写清楚。

## Phases

- [x] 阅读参考项目，提炼最小保留流程
- [x] 初始化项目结构与基础文档
- [x] 增加最小代码骨架：配置、日志、LLM 调用、CLI
- [in_progress] 自检仓库内容并提交 Git
- [ ] 推送到远程 `origin`

## Decisions

- 暂不复刻自动 query 调度、全量采集和多数据库设计
- 先以 `sample.json` 为统一输入单元
- 先做“主模型标注 + 规则回流 + 人工抽检”的质量闭环
- 本地视频默认不直接上传给模型，优先使用关键帧和 transcript

## Errors Encountered

| Error | Attempt | Resolution |
|-------|---------|------------|
| 参考项目路径包含空格，首次 `sed` 读取失败 | 1 | 改为带引号的路径读取 |
